apiVersion: v1
kind: Service
metadata:
  # the name here should match the name of the Service
  name: cerebro-gui-test
spec:
  selector:
    app: cerebro
  ports:
    - protocol: TCP
      port: 9000
      targetPort: 9000